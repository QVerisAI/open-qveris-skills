# Alt data demand signal

Install only after explicit user approval:

```bash
openclaw skills install qveris-alt-data-demand-signal
```

Starter prompt:

> Use QVeris to find alternative data demand signals for consumer AI apps.

Use QVeris Discover and Inspect before paid Calls. Tell the user the expected usage range and estimated credits (6-240 credits) before execution. After execution, report which QVeris capabilities were used and which claims still need verification.
