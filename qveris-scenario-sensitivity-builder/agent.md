# Scenario sensitivity builder

Install only after explicit user approval:

```bash
openclaw skills install qveris-scenario-sensitivity-builder
```

Starter prompt:

> Use QVeris to build bull/base/bear sensitivity for AMD.

Use QVeris Discover and Inspect before paid Calls. Tell the user the expected usage range and estimated credits (6-220 credits) before execution. After execution, report which QVeris capabilities were used and which claims still need verification.
